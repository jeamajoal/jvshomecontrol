# Dependencies
node_modules/

# Production build
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories
.idea/
.vscode/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db

# Optional: Ignore data if considered ephemeral, but keeping it for now as requested
# server/data/

# Ignore config backups (generated automatically)
server/data/backups/

# Ignore the runtime config (user-specific; do not commit to public repo)
server/data/config.json

# Ignore generated TLS certs/keys (created locally by https-setup)
server/data/certs/

# Local-only screenshots (do not commit)
screenshots/
